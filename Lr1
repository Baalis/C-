Вариант 11

#include <iostream>
#include <cmath> // Для математических функций

int main() {
    double a = 1.0;
    double b = 20.01;
    
    // Вычисление значения y по формуле
    double y = 0.719 * ((b + pow(a, 2)) / cos(pow(log(b), 2))) + (2 * b) / (6 * a + pow(b, 2));
    
    // Вывод результата
    std::cout << "Значение y для варианта 11: " << y << std::endl;

    return 0;
}

Вариант 16
#include <iostream>
#include <cmath> // Для математических функций

int main() {
    double a = 3.0;
    double b = 0.501;
    
    // Вычисление значения y по формуле
    double y = 3 * (a - b) * sin(M_PI * a) + cos(M_PI * b) * log(b);
    
    // Вывод результата
    std::cout << "Значение y для варианта 16: " << y << std::endl;

    return 0;
}
